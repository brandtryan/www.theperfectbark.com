// const animation = sampleElements.map(Element => Element.animate(keyframes, timing));
function readingTimeLine() {
  const words = wordArray0.length / 2;
  const time = words / 225 * 60;
  console.log(time);
}
readingTimeLine();